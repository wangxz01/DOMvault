"""DOMVault — lightweight local Playwright tool for capturing rendered DOM HTML.

A local web control panel that opens a real (headed) Playwright browser, lets the
user manually interact with a page, and saves the current rendered HTML on demand.
For scraping learning, selector debugging, and WebRPA flow analysis.
"""

__version__ = "0.3.0"
